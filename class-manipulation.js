let h1 = document.querySelector("h1");
h1.classList.add("abc"); //it add the class into the html tag
h1.classList.remove("abc"); //it remove the class into the html tag
h1.classList.toggle("abc"); 
// toggle : if class is existed the it remove the class or. 
// if class isn't existed then it add the class in the html.